export const API_URL = "http://localhost:3000/api/v1"; // API URL (backend)
export const TOKEN_KEY = "token";
export const REFRESH_TOKEN_KEY = "refresh-token";
export const TOKEN_EXPIRES_AT_KEY = "token-expires";
export const TIME_THRESHOLD = 60 * 60 * 1000; // Time threshold in milliseconds (e.g., 1 hour) for refresing token